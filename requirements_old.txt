# Essenciais para Django
Django==5.2
sqlparse==0.5.3
asgiref==3.8.1

# Essenciais para Tailwind CSS (assumindo que o app 'theme' usa esta biblioteca)
django-tailwind

# Para manipulação de imagens/arquivos (ex: comprovantes, fotos de perfil)
Pillow==11.2.1

# Para formatação de números nos templates (ex: valores monetários)
# django.contrib.humanize já vem com o Django

# Para suporte a Timezone (usado pelo Django)
pytz==2025.2
tzdata==2025.2

# Whitenoise para servir arquivos estáticos em produção (recomendado)
whitenoise
psycopg2-binary==2.9.10
python-dateutil==2.9.0.post0
six==1.17.0
python-dotenv==1.1.0
dj-database-url

openpyxl